pre-commit:
  parallel: true
  commands:
    format:
      glob: 'app/**/*.{ts,vue}'
      run: bunx oxfmt --check {staged_files}
    lint:
      glob: 'app/**/*.{ts,vue}'
      run: bunx oxlint {staged_files}

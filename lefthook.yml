pre-commit:
  parallel: true
  commands:
    format:
      glob: '{app,packages,apps}/**/*.{ts,tsx,vue}'
      run: bunx oxfmt --check {staged_files}
    lint:
      glob: '{app,packages,apps}/**/*.{ts,tsx,vue}'
      run: bunx oxlint {staged_files}

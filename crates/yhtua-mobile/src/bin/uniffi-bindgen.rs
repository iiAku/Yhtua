// Locked workspace binding generator: `cargo run -p yhtua-mobile --features
// bindgen --bin uniffi-bindgen -- generate ...` — pinned by Cargo.lock, never
// a floating `cargo install`.
fn main() {
    uniffi::uniffi_bindgen_main()
}
